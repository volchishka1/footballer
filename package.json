{
  "dependencies": {
    "@craco/craco": "^6.4.3",
    "axios": "^0.26.1",
    "dotenv": "^16.0.0",
    "env-cmd": "^10.1.0",
    "lerna": "^4.0.0"
  },
  "name": "footballer",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/**",
      "components/**"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "scripts": {
    "bootstrap": "npx lerna clean --yes && npx rimraf node_modules && npx lerna bootstrap",
    "startweb": "lerna run --scope @footballer/web start",
    "startmobileserv": "lerna run --scope @footballer/mobile start",
    "startapp": "lerna run --scope @footballer/mobile android"
  }
}
